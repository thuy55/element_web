import { createContext, useContext } from "react";
const ReleaseAnnouncementContext = createContext(null);
function useReleaseAnnouncementContext() {
  const context = useContext(ReleaseAnnouncementContext);
  if (context == null) {
    throw new Error(
      "ReleaseAnnouncement components must be wrapped in <ReleaseAnnouncement />"
    );
  }
  return context;
}
export {
  ReleaseAnnouncementContext,
  useReleaseAnnouncementContext
};
//# sourceMappingURL=ReleaseAnnouncementContext.js.map
